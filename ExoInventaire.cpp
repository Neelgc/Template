#include <iostream>
#include <vector>
#include <list>
#include <set>
#include <algorithm>
/*
class Item {
public:
	int poids;
	std::string nom;

	Item(std::string _nom, int _poids) : nom(_nom), poids(_poids)

};
	

	class inventory {
	public:
		std::vector<Item> items;
		void additem(std::string nom, int poids) {
			items.push_back(Item(nom, poids));
		}
		void deleteitem(std::string nom) {
			for (int i = 0; i < items.size(); i++) {
				if (items[i].nom == nom) {
					items.erase(items.begin() + i);
				}
			}
		}

		void sortname() {
			std::sort(items.begin(), items.end()), [](const Item& a, const Item& b) {
				return a.nom < b.nom;
				};
		};

		void sortweight() {
			std::sort(items.begin(), items.end()), [](const Item& a, const Item& b) {
				return a.poids < b.poids;
				};
		};

		void print() {
			int totalweight = 0;
			for (Item item : items) {
				std::cout << item.nom << "Weight :" << item.poids << std::endl;
				totalweight += item.poids;

			}
		}

	};

	int main() {}

	*/

//	std::vector<int> inventaire;
	//std::string string;
